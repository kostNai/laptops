export type CpuType = {
    id?: string
    manufacturer: string
    series: string
    model: string
    cores_value: number
    frequency: number
    slug: string
    product_id: number
    created_at?: string
    updated_at?: string
}
